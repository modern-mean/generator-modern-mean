{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "keywords": "<%= keywords %>",
  "main": "index.js",
  "scripts": {
    "coverage": "gulp test:coverage",
    "test": "gulp test",
    "build": "gulp",
    "preversion": "npm test",
    "version": "npm run build && git add -A",
    "postversion": "git push && git push --tags && npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "git+<%= gitbase %>.git"
  },
  "author": "<%= author %>",
  "license": "<%= license %>",
  "bugs": {
    "url": "<%= gitbase %>/issues"
  },
  "homepage": "<%= gitbase %>#readme",
  "dependencies": { }
}
